# cbindgen configuration for hyprslog FFI

language = "C"

# Header settings
header = """
/**
 * hyprslog - Flexible logging library
 *
 * C-ABI bindings for hyprslog.
 *
 * Basic usage:
 *   HyprlogContext* ctx = hyprslog_init();
 *   hyprslog_info(ctx, "MAIN", "Application started");
 *   hyprslog_free(ctx);
 *
 * Or with simple setup (no config file):
 *   HyprlogContext* ctx = hyprslog_init_simple(HYPRLOG_LEVEL_DEBUG, 1);
 *   hyprslog_debug(ctx, "NET", "Connecting...");
 *   hyprslog_free(ctx);
 */
"""

include_guard = "HYPRLOG_H"
pragma_once = true
include_version = true
autogen_warning = "/* Auto-generated by cbindgen. Do not edit manually. */"

# Style
style = "both"
usize_is_size_t = true
cpp_compat = true  # Adds extern "C" { } for C++ compatibility

[defines]

[export]
include = ["HyprlogContext"]
exclude = []

[export.rename]

[enum]
rename_variants = "ScreamingSnakeCase"

[fn]
rename_args = "SnakeCase"

[struct]
rename_fields = "SnakeCase"

[const]

[macro_expansion]
bitflags = true

[parse]
parse_deps = false
